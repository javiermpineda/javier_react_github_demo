{
  "devDependencies": {
    "jest": "^29.7.0"
  }
}
{
  "scripts": {
    "dev": "vite",
    "lint": "eslint --ext .jsx --fix src",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run"
  },}